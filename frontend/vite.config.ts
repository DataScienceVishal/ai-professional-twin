/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      // The FastAPI routes are NOT prefixed (`/chat`, `/projects`, `/skills`,
      // `/health`, `/resume/download`), so strip `/api` before forwarding.
      // This mirrors the Vercel rewrite in `vercel.json`, so local dev and
      // production behave identically and no env var is needed in either.
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
})
